var length = 5;//prompt("Enter length");
var breadth =  6;//prompt("Enter breadth");
var height = 7;//prompt("Enter height");

function area(length, breadth, height){
      return 1/2*length*breadth*height;
}

var ar = area(length, breadth, height);
console.log(ar);

